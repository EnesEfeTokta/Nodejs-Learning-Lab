# 05 HTTP IP Info
In this project, we will create a simple Node.js application that fetches and displays the geographical location (country and city) of the user's IP address using a public API.

## Concepts covered
- Making HTTP requests in Node.js